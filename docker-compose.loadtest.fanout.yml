# ─────────────────────────────────────────────────────────────────────────────
# GENERATED by scripts/loadtest-fanout.mjs — DO NOT HAND-EDIT (regenerate instead).
#   N=20 canvases · gateway-<i> host ports 8100..8119 · canvas loadtest-<i>
#
# MULTI-CANVAS disposable load-test stack (FEN-516). Drives the graven
# 20-toiles × 500 = 10 000 conns target as N GENUINELY DISTINCT canvases — each
# with its own gateway (own host port) AND its own worker drain loop — over ONE
# shared, disposable Redis + Convex backend. This is the multi-canvas counterpart
# to the single-canvas docker-compose.loadtest.yml (which is a mono-canvas
# ceiling). NEVER PRODUCTION: own project name, namespaced volumes, .env.loadtest
# only, no web/proxy/TLS. See docs/runbooks/loadtest-env.md.
#
# Bring up:  ./scripts/loadtest.sh fanout-up
# Targets:   .env.loadtest-fanout-targets  (TARGET_URLS for the campaign runner)
# ─────────────────────────────────────────────────────────────────────────────
name: liveplace-loadtest-fanout

services:
  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes", "--appendfsync", "everysec"]
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  convex-backend:
    image: ghcr.io/get-convex/convex-backend:latest
    env_file: [.env.loadtest]
    environment:
      - INSTANCE_NAME=liveplace-loadtest
      - INSTANCE_SECRET=${CONVEX_INSTANCE_SECRET}
      - CONVEX_CLOUD_ORIGIN=http://localhost:3210
      - CONVEX_SITE_ORIGIN=http://localhost:3211
    volumes:
      - convex-data-2:/convex/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3210/version"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    restart: unless-stopped

  convex-admin-key:
    build:
      context: .
      dockerfile: infra/convex-admin-key/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - INSTANCE_NAME=liveplace-loadtest
      - INSTANCE_SECRET=${CONVEX_INSTANCE_SECRET}
    volumes:
      - convex-admin:/admin
    depends_on:
      convex-backend:
        condition: service_healthy
    restart: "no"

  convex-deploy:
    build:
      context: .
      dockerfile: apps/convex/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
    volumes:
      - convex-admin:/admin:ro
    depends_on:
      convex-backend:
        condition: service_healthy
      convex-admin-key:
        condition: service_started
    restart: "no"

  gateway-0:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-0
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8100:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-1:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-1
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8101:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-2:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-2
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8102:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-3:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-3
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8103:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-4:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-4
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8104:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-5:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-5
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8105:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-6:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-6
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8106:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-7:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-7
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8107:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-8:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-8
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8108:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-9:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-9
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8109:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-10:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-10
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8110:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-11:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-11
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8111:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-12:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-12
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8112:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-13:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-13
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8113:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-14:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-14
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8114:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-15:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-15
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8115:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-16:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-16
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8116:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-17:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-17
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8117:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-18:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-18
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8118:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  gateway-19:
    image: liveplace-loadtest-fanout/gateway
    build:
      context: .
      dockerfile: apps/gateway/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - GATEWAY_CANVAS_ID=loadtest-19
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
      - CONVEX_JWKS_URL=http://convex-backend:3211/api/auth/convex/jwks
      - GATEWAY_JWT_AUDIENCE=convex
    ports:
      - "8119:8080"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
    restart: unless-stopped

  worker-0:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-0
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-1:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-1
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-2:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-2
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-3:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-3
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-4:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-4
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-5:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-5
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-6:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-6
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-7:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-7
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-8:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-8
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-9:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-9
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-10:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-10
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-11:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-11
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-12:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-12
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-13:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-13
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-14:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-14
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-15:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-15
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-16:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-16
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-17:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-17
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-18:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-18
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

  worker-19:
    image: liveplace-loadtest-fanout/worker
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    env_file: [.env.loadtest]
    environment:
      - REDIS_URL=redis://redis:6379
      - CONVEX_SELF_HOSTED_URL=http://convex-backend:3210
      - GATEWAY_CANVAS_ID=loadtest-19
      - GATEWAY_INTERNAL_SECRET=${GATEWAY_INTERNAL_SECRET:-}
    depends_on:
      redis:
        condition: service_healthy
      convex-backend:
        condition: service_healthy
    restart: unless-stopped

volumes:
  # DISPOSABLE — `down -v` (./scripts/loadtest.sh fanout-clean) removes them.
  redis-data:
  convex-data-2:
  convex-admin:
